name: Deploy to Vercel

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy to Vercel (via webhook)
        run: |
          echo "🚀 Triggering Vercel deployment..."
          # Vercel's Git integration will auto-deploy on push
          # This workflow just confirms the deployment started
          echo "✅ Push detected. Vercel should auto-deploy."
          echo "⏳ Waiting 30 seconds for deployment..."
          sleep 30
          echo "🌐 Site: https://velum-five.vercel.app"

      - name: Check deployment status
        run: |
          echo "🔄 Check your site: https://velum-five.vercel.app"
          echo "📊 Vercel Dashboard: https://vercel.com/hrimiucmihai-gmailcoms-projects/velum"
